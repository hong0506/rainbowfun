import { render } from "@testing-library/react";
import { PageFooter } from "./PageFooter";

describe("PageFooter", () => {
  it("render PageFooter component", () => {
    render(<PageFooter />);
  });
});
