import * as React from "react";

import * as Styled from "./PageFooter.styled";

export const PageFooter: React.FunctionComponent = () => {
  return <Styled.PageFooterContainer>example text</Styled.PageFooterContainer>;
};
