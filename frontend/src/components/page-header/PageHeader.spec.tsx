import { render } from "@testing-library/react";
import { PageHeader } from "./PageHeader";

describe("PageHeader", () => {
  it("render PageHeader component", () => {
    render(<PageHeader />);
  });
});
