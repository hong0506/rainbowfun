import * as React from "react";

import * as Styled from "./PageHeader.styled";

export const PageHeader: React.FunctionComponent = () => {
  return <Styled.PageHeaderContainer>example text</Styled.PageHeaderContainer>;
};
