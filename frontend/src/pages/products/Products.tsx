import * as React from "react";

import * as Styled from "./Products.styled";

export const Products: React.FunctionComponent = () => {
  return <Styled.ProductsContainer>Products page</Styled.ProductsContainer>;
};
